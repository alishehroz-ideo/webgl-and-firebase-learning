Drop your real screenshots here with these EXACT names, then rebuild the deck
(run  video\build-deck.ps1 ). Any missing file shows a labelled placeholder box,
so the deck always builds.

OVERVIEW slide — one real screen per step:
  shelf-screen.png     The shelf / Main Menu in the browser (saved book covers)
  editor-screen.png    The BookLab editor, mid-build (background + a few stickers)
  playback-screen.png  A saved book OPEN in playback (a finished page showing)

ARCHITECTURE slide:
  project-folder.png   Unity -> the Assets/_Project folder tree, expanded
                       (App, Core, Editor, Features, Models, Services visible)

DATA slide:
  firebase-data.png    Firebase console -> Realtime Database, a saved book
                       expanded under  books/demo/...  (so the JSON recipe shows)

Tip: landscape (roughly 16:10) crops look best. Images are embedded straight into
the deck as data-URIs, so no internet is needed when recording.
